const xxxxController = require("express").Router();
const xxxxService = require("../services/xxxxService");
const { parseError } = require("../util/parser");

// xxxxController.get("/", (req, res) => {});
// xxxxController.post("/", (req, res) => {});

module.exports = xxxxController;
